services:
  gestion-formation:
    build: .
    image: gestion-formation:local
    ports:
      - "8000:8000"
    environment:
      DATABASE_PATH: /app/data/bdd_formations.db
    volumes:
      - gestion-formation-data:/app/data

volumes:
  gestion-formation-data:
